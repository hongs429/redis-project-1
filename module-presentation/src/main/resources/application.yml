spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3309/redis-movie?useSSL=false&allowPublicKeyRetrieval=true
    username: hongs
    password: local1234
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    show-sql: true

logging:
  level:
    org:
      hibernate:
        SQL: info
        type:
          descriptor:
            sql:
              info